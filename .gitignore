# 교재 원본 PDF — 앱 저장소에는 포함하지 않음 (폴더에는 그대로 보관됨)
*.pdf

# OS / 편집기 부산물
.DS_Store
Thumbs.db
desktop.ini

# 로그
*.log

# 검수 파이프라인 생성물 (audit/run.mjs 로 재생성)
audit/out/
audit/verdicts/*.template.json

# ElevenLabs 음성 파일 — voice/generate.mjs 로 만든다 (약 76 MB · 9,900개)
# 저장소에 넣으면 클론이 무거워진다. 배포할 때 함께 올릴 것.
# GitHub Pages 로 배포하면 저장소에 있어야 하므로 아래 두 줄을 지운다.
voice/audio/
voice/manifest.json
voice/failed.json
