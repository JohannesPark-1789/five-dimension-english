# 교재 원본 PDF — 앱 저장소에는 포함하지 않음 (폴더에는 그대로 보관됨)
*.pdf

# OS / 편집기 부산물
.DS_Store
Thumbs.db
desktop.ini

# 로그
*.log

# 검수 파이프라인 생성물 (audit/run.mjs 로 재생성)
audit/out/
audit/verdicts/*.template.json

# ElevenLabs 음성 파일 — voice/generate.mjs 로 만든다 (약 57 MB · 9,900개)
#
# GitHub Pages 로 배포하므로 voice/audio/ 와 voice/manifest.json 은 저장소에
# 들어간다 (일부러 여기서 빼 두지 않았다). 모델(voice/config.mjs)은 커밋 전에
# 정할 것 — 파일 이름이 문장 해시라 모델을 바꿔도 이름은 그대로여서, 다시
# 만들면 57 MB 가 히스토리에 또 쌓인다.

# 시험 변환·비교용 — 배포물이 아니다
voice/compare/
voice/failed.json
